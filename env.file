# URL to use in GitHub PRs link
JENKINS_URL=URL

# Token used to publish checks in GitHub
GITHUB_TOKEN=token

# Credentials used for publishing image on docker hub (not mandatory)
#DOCKER_USER=user
#DOCKER_PASSWORD=pass
